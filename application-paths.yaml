api:
  paths:
    version: /api/v1

    #------------------ Authentication ------------------
    public:
      base: ${api.paths.version}/public
      categories: /operation/categories
      specifications: /operation/specifications
      
    auth:
      base: ${api.paths.version}/auth
      signup: /signup
      signin: /signin
      signout: /signout
      verify-email: /verify-email
      forgot-password: /forgot-password
      reset-password: /reset-password
      resend-verification: /resend-verification

    admin-auth:
      base: ${api.paths.version}/admin/auth
      signup: /signup
      delete-unverified-accounts: /delete-unverified-accounts

    custom-auth:
      base: ${api.paths.version}/custom/auth
      find-user: /find-userId

    #------------------ REDIS Blacklist ------------------
    blacklist:
      base: ${api.paths.version}/blacklist
      add-jwt: /add-jwt
      find-jwt: /find-jwt
      delete-all-jwt: /delete-all-jwt

    cache-update-categories:  ${api.paths.version}/update/cache-categories
    cache-update-specifications:  ${api.paths.version}/update/cache-specifications
      
    #----------------- Administration -----------------
    

    #----------------- Operation -----------------
    admin-operation:
      base-categories: ${api.paths.version}/admin/operation/categories
      add-categories: /add-categories
      add-subcategories: /add-subcategories
      add-products: /add-products
      update-categories: /update-categories
      update-subcategories: /update-subcategories
      update-products: /update-products
    
      base-specifications: ${api.paths.version}/admin/operation/specifications
     
    public-operation:
      base: ${api.paths.public.base}/operation
      categories: /categories
      add-categories: /find-categories
      add-subcategories: /find-subcategories
      add-products: /find-products

    #------------------ REDIS ------------------
    #------------------ Notification ------------------
    notification:
      base: ${api.paths.version}/notification
      confirm-registration: /confirm-registration
      reset-password: /reset-password
      passwordchanged: /passwordchanged
      temporary-password: /temporary-password
